# grade-distribution-c
C program that classifies student grades into letter categories (A–F) and displays the distribution.

## Features
- Uses loops
- Uses conditional statements
- Validates input range (0–100)
- Displays grade distribution
